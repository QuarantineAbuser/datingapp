TO DO:
- Fix looks
- Make role choosing in registry only for logged in admins(hide it for users)
- Add better labels for register page
- Maybe add pictures to users?
- 18+ users only
- maybe add block function(cant like or interest them), they cant see your profile
- Add calendar choosing date of birth, which calculates the age in the end
- Make input checks like phonenumbers and stuff
- (DONE) When trying to /login or /register while logged in redirect them
back to main page
- Filter posts by some date, user, sort by likes.
- Static User in data initializer on every login so I don't use requests all the time.
- Maybe choose if post is only for matched
- Message system with String where I append -> ActiveUser: + "content\n"
- (DONE) Add apostrophe to profile timeline
- (DONE) Add link to users name top right
- Fix redirects to pages
- (DONE) Error when liking more than 1 post
- Filter posts by keywords